TODO — things to do to the next prompt
======================================

Write the thought down the moment you have it, in whichever section it
belongs to. Delete the line once it is done. This file is meant to be
messy. Nothing in it is finished work; everything in it eventually moves
somewhere that is.

An hour after you have the thought, you no longer have the thought. That
is the whole reason this file exists.

This is the instructor's copy, and it is the same file students get in
the project template — the practice is not worth teaching if it is not
worth keeping.


## Change in the prompt
Something the next version of a file in prompts/ has to do differently.

- [ ] 03-design-schedule.md: say explicitly that labs assign no reading.
      It has had to be corrected in every run so far.

- [ ] every prompt that touches a lab has to say the browser is the
      supported path now. The install-first habit is baked into the
      older prompts and it came back twice while doing ADR-020.


## Change in data/
A fact that is wrong, missing, or stated in prose where it should be a
field. Course content lives in data/*.yml and nowhere else.

- [ ] check est_minutes on the Week 2 readings against how long they
      actually took. NB: w02-tue is pinned at exactly 241 in
      HEAVY_SESSIONS, so re-timing fails the build until that number is
      updated deliberately. That is the point of it.

- [ ] seven lab notebooks are still drafts: w04 w06 w07 w08 w11 w12
      w13. Structure is settled, code is not written. Each one must be
      RUN END TO END before its status flips to complete -- w02 had two
      factually wrong paragraphs that only running it caught.

- [ ] THE NEW w03-thu HAS NEVER BEEN OPENED IN COLAB and it is now the
      primary lab. Every cell was run end to end on CPU locally,
      INCLUDING the bertviz cell in section 7 -- model_view accepts the
      T5 encoder/decoder/cross tuples and returns its HTML, checked with
      html_action="return". What is still untested is Colab itself: its
      library versions and whether the widget renders in that iframe.

- [ ] the 1 GB flan-t5-base download on one room's wifi is untested.
      Same worry as glove-wiki-gigaword-50 in w02-thu, three times the
      size. Consider telling students to run the setup cell before they
      arrive.

- [ ] w03-thu-translation (the deep dive) is complete and every cell has
      been run, but NOT in Colab
      -- it was run locally on the same library versions. Open it in
      Colab once before the session. The only cell with real Colab risk
      is the bertviz widget in section 7, which is why nothing above it
      depends on bertviz.

- [ ] THE STUDY GUIDE STOPS AT WEEK 2 AND IT IS NOW THE EXAM. All 47
      concepts are filed to w01-tue and w02-tue. Nothing from Week 3
      exists: no attention, query/key/value, multi-head, positional
      encoding, masking, cross-attention. Since ADR-023 the concept
      check may only ask what is on that page, so as it stands the
      Week 4 meeting cannot ask a single question about the paper the
      course is named after. Write those entries before Sep 15.
      Each needs definition + mastery + assessed_in: [concept-checks].
      The Week 3 deck footers already name the entries they teach --
      that is the list to work from.

- [ ] concept checks and progress reports have no rubric. The
      assignments page says what happens in the meeting and what is
      graded, but not what separates a strong answer from a weak one.
      Write it before the first meeting, or the first grade is
      unjustifiable.

- [ ] w08-thu (red-teaming) may not want a notebook at all -- most of
      that session happens on hackaprompt.com. Decide before Week 8
      whether it is a logging notebook or a notebook_exempt.


## Slides
Decks in flight. Generated media is never committed; the scripts are.

- [ ] W03 is 31 slides and the speaker notes carry a clock from 0:00 to
      1:16. THAT CLOCK IS A GUESS. Time the first run and write the real
      numbers here. The review is budgeted at 20 min and the five
      discussion stops at 4-5 min each, which is 25 min of the 55 left
      -- if the room argues well, something has to give, and it should
      be s25 (the block) rather than a discussion.

- [ ] s14b-s17b run on a two-number toy model (embeddings and three 2x2
      lenses at the top of gen_w03_05_attention.py) and say so on the
      slide; s22's scores are illustrative too. Every number on those
      slides is computed from the toy, so changing one embedding moves
      the whole sequence. If a real head ever turns out legible enough
      -- coreference at layer 3 rather than 5 -- it could replace the toy,
      but 64-number vectors will not fit on the score slide.

- [ ] w03_s08 (perceptron) and w03_s09 (CNN) say "not a study-guide
      entry" on the slide. If that ever stops being true they need
      concepts.yml entries with mastery + assessed_in, not just a
      footer edit.


## Syllabus PDF
The filed copy is a build artifact. It goes stale silently.

- [ ] RE-RUN scripts/build_syllabus_pdf.py and re-file before the term
      starts, and after any change to semester.yml or schedule.yml. There
      is no test that can catch a stale PDF sitting in someone's inbox.

- [ ] check with the registrar whether anything else is required boilerplate
      -- the F26 210.02 syllabus only had the disability-services statement,
      but that was one course's document, not a College template. Ask for
      the actual template if there is one.


## Worth an ADR
A choice a reader could not reconstruct from the files themselves.
Write it up in docs/adr/index.md while the rejected alternatives are
still fresh — that section is the part that is worth anything.

- [ ] hosting a deck the site does not show yet is a real choice --
      deck_order.py grew a publish flag for it. Worth an ADR if a
      second deck ever sits unpublished for more than a week.

- [ ] if Colab's free tier changes, the fallback (Kaggle? Binder?) is a
      real decision and should be written up before it is needed rather
      than during the week it breaks.


## Ask about
Open questions, and anything to verify before it goes in front of
students.

- [ ] does the project template still assume a local clone? Its README
      walks through venv + API key, and Week 1 no longer does either.
      It is a separate repo, so it did not move with the rest of this.

- [ ] gensim needs a Python version with a prebuilt wheel. Colab is on
      3.12 and fine; it does NOT build on 3.14. Worth re-checking in
      August in case Colab has moved.

- [ ] w02-thu wants a real check that thirteen students can all pull
      glove-wiki-gigaword-50 in one room without the wifi dying.
